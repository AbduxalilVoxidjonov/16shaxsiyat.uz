import { PlaceholderPage } from '@/shared/ui/PlaceholderPage';

/** Skelet bosqichi — to'liq mantiq keyingi promptlarda qo'shiladi (prompts/19, prompts/29). */
export default function SettingsPage() {
  return <PlaceholderPage titleKey="pages.settings.title" />;
}
